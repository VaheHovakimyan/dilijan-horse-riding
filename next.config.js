/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone", // Keeps the app bundled into a single executable for production
  reactStrictMode: true,
};

module.exports = nextConfig;
